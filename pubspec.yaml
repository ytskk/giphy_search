name: giphy
description: A new Giphy Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  get_it: ^7.2.0
  dio: ^4.0.6
  flutter_bloc: ^8.1.1
  bloc_concurrency: ^0.2.0
  stream_transform: ^2.0.0
  equatable: ^2.0.5
  freezed_annotation: ^2.1.0
  json_annotation: ^4.7.0
  cached_network_image: ^3.2.2
  flutter_staggered_grid_view: ^0.6.2
  scrolls_to_top: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  freezed: ^2.1.1
  json_serializable: ^6.4.1

flutter:

  uses-material-design: true